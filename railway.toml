[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm ci --omit=dev || npm install --omit=dev"

[deploy]
startCommand = "cd backend && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
